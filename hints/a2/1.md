**A2 — hint 1 of 2**

The task succeeded. That tells you the HTTP call worked.

Does it tell you anything at all about *which delivery you got*?

Compare what you asked for with what the response says it is. Both values are
already in the dict `fetch_delivery` returns.
