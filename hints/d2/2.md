**D2 — hint 2 of 2**

Read the bounds from `config/team.yaml` via `cfg.is_plausible(total)`. If your
check would also pass a €12 day, it is not a data contract yet.

`load.total_loaded_revenue(BUSINESS_DATE, schema=cfg.schema)` reads back what was
actually loaded.

Return the numbers in the result, not just a boolean:

```python
return AssetCheckResult(
    passed=passed,
    severity=AssetCheckSeverity.ERROR,
    description=f"EUR {total:,.2f} for {BUSINESS_DATE}",
    metadata={"total_revenue_eur": ..., "min_eur": ..., "max_eur": ...},
)
```

Someone reads this at 07:00 with no context.
