**D1 — hint 2 of 2**

Return `MaterializeResult(metadata={...})` rather than a plain value or `None`.

```python
return MaterializeResult(metadata={
    "total_revenue_eur": MetadataValue.float(float(total)),
    "store_count": len(rows),
})
```

Then open `daily_revenue` in the catalog and **read the number you attached.**

That number is the mission. Nothing here fails — if you are waiting for an error,
you are waiting for something that is not coming.
