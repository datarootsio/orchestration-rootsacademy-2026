**A4 — hint 2 of 2**

For the gate: `cfg.is_plausible(total)` reads **your team's** bounds from
`config/team.yaml`. If your check would also pass a €12 day, it is not a data
contract yet.

For the asset: an `Asset` is Airflow's way of saying *"this dataset now exists,
and here is what I know about it."*

```python
@task(outlets=[DAILY_REVENUE], retries=0)
def publish_daily_revenue(validated: dict, **context) -> dict:
    ...
    context["outlet_events"][DAILY_REVENUE].extra = {...}
```

Emit it only when the data is sound, and a downstream DAG can be scheduled on the
data instead of on a time.

Remember `semantic_gate_enforced` needs **both** directions — run 2026-03-04
(must fail) and 2026-03-03 (must pass).
