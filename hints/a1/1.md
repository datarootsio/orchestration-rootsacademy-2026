**A1 — hint 1 of 2**

Airflow already has somewhere to keep secrets. Look at **Admin → Connections**,
and note the exact connection ID the stub expects — it is in `SUPPLYHUB_CONN_ID`.

`roots airflow-conn` prints every field for you, with the real values.
