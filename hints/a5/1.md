**A5 — hint 1 of 2**

You already emit the event in A4. A DAG can be scheduled on it instead of on a
cron string:

```python
@dag(schedule=[Asset(name="rootsmarkt_daily_revenue",
                     uri="rootsmarkt://warehouse/daily_revenue")], ...)
```

The interesting question is the one the mission asks: **what triggers the
consumer when the data is bad?**
