**A3 — hint 1 of 2**

You will run this pipeline more than once today, and so will the real one — every
night.

What must be true about the load step for the second run to be safe?

Look at the primary key on the `daily_revenue` table. It is the hint hiding in
plain sight, and `load.load_daily_revenue()` already honours it. The lesson is
noticing that it matters.
